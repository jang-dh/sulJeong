<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fundingQuestionMapper">

	<select id="fundingQuestionList" parameterType="int" resultType="fundingQuestion">
		select code, funding_code, member_code, content, regdate, state from funding_question
		where member_code=#{_parameter}
	</select>
</mapper>	









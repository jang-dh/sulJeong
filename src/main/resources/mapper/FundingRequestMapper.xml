<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fundingRequestMapper">

<insert id="fundingInsert" parameterType="fundingRequest">
insert into funding_request(CODE,MEMBER_CODE,SUBJECT,FILENAME,STATE,REGDATE)
values(SEQ_NO.nextval,#{memberCode},#{subject},#{filename},'100',SYSDATE)
</insert>


<select id="myFundingOpenList" resultType="funding" parameterType="int">
		select code, md_code, title, reward_name, reward_price, goal_price, stack_price, image, category, open_date, end_date, funding_state
		from funding
		where md_code = #{_parameter}
</select>
</mapper>	









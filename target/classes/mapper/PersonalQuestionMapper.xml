<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="personalQuestionMapper">

	<select id="personalQuestionList" parameterType="int" resultType="personalQuestion">
		select code, member_code, content, subject, regdate, state from personal_question
		where member_code=#{_parameter}
	</select>
</mapper>	








